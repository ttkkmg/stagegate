# minigate

`minigate` is the shell-command-oriented user front end for `stagegate`.

It lets you describe forward-only shell-command pipelines in a small DSL and
execute them on top of the `stagegate` scheduler.

```{toctree}
:maxdepth: 1

quickstart
cli
pipeline-files
templates
input-formats
examples
```

Public sample files live under [examples/](./examples/).
